(function (root,factory){if(typeof define==='function'&&define.amd){define([],factory);}else if(typeof module==='object'&&module.exports){module.exports=factory();}else{root.ProtocolUtils=factory();}}(typeof self!=='undefined'?self:this,function (){const ProtocolUtils={hex:function (str){return new Uint8Array(str.match(/.{1,2}/g).map(byte=>parseInt(byte,16)));},hexArr:function (data){return Array.from (data).map(b=>b.toString(16).padStart(2,'0').toUpperCase()).join(' ');},createFrame:function (header,data,lengthOverride){let len=(lengthOverride!==undefined)?lengthOverride:data.length;let packet=[0x16,0x10,header,len];let crcLoad=[len];let stuffedData=[];for(let b of data){crcLoad.push(b);if(b===0x10)stuffedData.push(0x10,0x10);else stuffedData.push(b);}let crc=this.calculateCRC(crcLoad);return new Uint8Array([...packet,...stuffedData,0x10,0x03,crc&0xFF,(crc>>8)&0xFF]);},calculateCRC:function (buffer){let crc=0x0000;const poly=0xA001;for(let i=0;i<buffer.length;i++){crc^=buffer[i];for(let j=0;j<8;j++){if(crc&1)crc=(crc>>>1)^poly;else crc=crc>>>1;}}return crc;},validatePacketCRC:function (hexStr){try{if(!hexStr||typeof hexStr!=='string')return {valid:false,error:"Invalid Input"};const match=hexStr.match(/.{1,2}/g);if(!match)return {valid:false,error:"No match"};let bytes=new Uint8Array(match.map(byte=>parseInt(byte,16)));if(bytes.length<4)return {valid:false,error:"Too short"};let crcGiven=(bytes[bytes.length-1]<<8)|bytes[bytes.length-2];let rawPayload=bytes.slice(3,bytes.length-4);let payload=[];for(let i=0;i<rawPayload.length;i++){payload.push(rawPayload[i]);if(rawPayload[i]===0x10&&i<rawPayload.length-1&&rawPayload[i+1]===0x10){i++;}}let crc=0x0000;const poly=0xA001;for(let i=0;i<payload.length;i++){crc^=payload[i];for(let j=0;j<8;j++){if(crc&1)crc=(crc>>>1)^poly;else crc=crc>>>1;}}if(crc!==crcGiven){console.log(`[CRC FAIL DEBUG] String: ${hexStr}`);console.log(`[CRC FAIL DEBUG] Bytes: ${bytes.length} Payload: ${payload.length}`);}return {valid:(crc===crcGiven),given:crcGiven,calc:crc};}catch(e){console.error("validatePacketCRC Exception:",e);return {valid:false,error:e.message};}},constructShortPacket:function (typeRef,dataLen){let crcPayload=[0x01,typeRef];let crc=this.calculateCRC(new Uint8Array(crcPayload));let pkt=[0x16,0x10,0x02,0x01,typeRef];if(typeRef===0x10)pkt.push(0x10);pkt.push(0x10,0x03,crc&0xFF,(crc>>8)&0xFF);return new Uint8Array(pkt);},constructLongPacket:function (typeRef,data){let crcPayload=[0x01,typeRef];for(let i=0;i<data.length;i++)crcPayload.push(data[i]);let crc=this.calculateCRC(new Uint8Array(crcPayload));let pkt=[0x16,0x10,0x02,0x01,typeRef];if(typeRef===0x10)pkt.push(0x10);for(let i=0;i<data.length;i++){pkt.push(data[i]);if(data[i]===0x10)pkt.push(0x10);}pkt.push(0x10,0x03,crc&0xFF,(crc>>8)&0xFF);return new Uint8Array(pkt);},byteArrayToHex:function (arr){return Array.from (arr).map(b=>b.toString(16).toUpperCase().padStart(2,'0')).join('');},calculateSimpleSum:function (data){let sum=0;for(let b of data)sum+=b;return sum>>>0;},calculateCRC:function (buffer){let crc=0x0000;const poly=0xA001;for(let i=0;i<buffer.length;i++){crc^=buffer[i];for(let j=0;j<8;j++){if(crc&1)crc=(crc>>>1)^poly;else crc=crc>>>1;}}return crc;},calculateCRC32:function (data){var table=new Uint32Array(256);for(var i=0;i<256;i++){var c=i;for(var j=0;j<8;j++){c=((c&1)?(0xEDB88320^(c>>>1)):(c>>>1));}table[i]=c;}var crc=0xFFFFFFFF;for(var i=0;i<data.length;i++){crc=(crc>>>8)^table[(crc^data[i])&0xFF];}return (crc^0xFFFFFFFF)>>>0;},applyBootCodeFixup:function (packetHex,targetAddr,fixups){let bytes=this.hex(packetHex);let baseHigh=bytes[5];let baseLow=bytes[6];let baseAddr=(baseHigh<<8)|baseLow;let delta=targetAddr-baseAddr;if(delta===0)return packetHex;fixups.forEach(offset=>{if(offset<bytes.length-1){let oldVal=(bytes[offset]<<8)|bytes[offset+1];let newVal=(oldVal+delta)&0xFFFF;let nb1=(newVal>>8)&0xFF;let nb2=newVal&0xFF;if(nb1===0x10||nb2===0x10){return;}bytes[offset]=nb1;bytes[offset+1]=nb2;}});console.log(`[FIXUP] Patching Header Base: 0x${baseAddr.toString(16).toUpperCase()} -> 0x${targetAddr.toString(16).toUpperCase()}`);bytes[5]=(targetAddr>>8)&0xFF;bytes[6]=targetAddr&0xFF;bytes[2]=0x02;bytes[3]=0x01;let crcStart=3;let crcEnd=bytes.length-4;let payloadBytes=[];for(let i=crcStart;i<crcEnd;i++){let b=bytes[i];payloadBytes.push(b);if(b===0x10&&bytes[i+1]===0x10){i++;}}let newCrc=this.calculateCRC(new Uint8Array(payloadBytes));bytes[bytes.length-2]=newCrc&0xFF;bytes[bytes.length-1]=(newCrc>>8)&0xFF;return this.hexArr(bytes).replace(/\s/g,'');},autoRelocateBootCode:function (hexStr,delta,minAddr,maxAddr){let bytes=this.hex(hexStr);if(bytes[0]!==0x16||bytes[1]!==0x10||bytes[2]!==0x02||bytes[3]!==0x01){return hexStr;}let type=bytes[4];let rawData=[];for(let i=5;i<bytes.length-4;i++){let b=bytes[i];rawData.push(b);if(b===0x10&&bytes[i+1]===0x10){i++;}}let patched=false;for(let i=0;i<rawData.length-1;i++){let val=(rawData[i]<<8)|rawData[i+1];if(val>=minAddr&&val<=maxAddr){let newVal=val+delta;console.log(`[RELOC] Patching 0x${val.toString(16).toUpperCase()} -> 0x${newVal.toString(16).toUpperCase()} at offset ${i}`);rawData[i]=(newVal>>8)&0xFF;rawData[i+1]=newVal&0xFF;patched=true;i++;}}if(!patched)return hexStr;let stuffedData=[];for(let b of rawData){stuffedData.push(b);if(b===0x10)stuffedData.push(0x10);}let crcPayload=[0x01,type];for(let b of stuffedData)crcPayload.push(b);let newCrc=this.calculateCRC(new Uint8Array(crcPayload));let frame=[0x16,0x10,0x02,0x01,type];for(let b of stuffedData){frame.push(b);}frame.push(0x10);frame.push(0x03);frame.push(newCrc&0xFF);frame.push((newCrc>>8)&0xFF);return this.hexArr(frame).replace(/\s/g,'');}};return ProtocolUtils;}));